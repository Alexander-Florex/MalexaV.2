# Datos de conexion a tu MySQL local (los mismos que usas en MySQL Workbench)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=percha_db
DB_USER=root
DB_PASSWORD=tu_password_de_mysql

# Servidor
PORT=4000

# JWT
JWT_SECRET=cambia_esto_por_un_secreto_largo_y_unico
JWT_EXPIRES_IN=8h

# CORS — URL donde corre el frontend
FRONTEND_URL=http://localhost:5173
